services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/event_cache:/app/event_cache

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
